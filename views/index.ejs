<!DOCTYPE html>
<html>

<head>
  <% include headerStuff %>
  </head>
  
  <body>
    <% include pageHeader %>
    <% if (notelist.length > 0) { %>
    <ul>
      <%for (var note of notelist) {%>
      <li>
        <%= note.keys %>:
        <a href="/notes/view?id=<%= note.id %>">
          <%= note.body %></a>
      </li>
      <%
  }
  %>
    </ul>
    <% } %>
    <% include footer %>
  </body>
  
  </html>